<p id="notice"><%= notice %></p>

<h1>Avisos</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @avisos.each do |aviso| %>
      <tr>
        <td><%= aviso.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Aviso', new_aviso_path %>
